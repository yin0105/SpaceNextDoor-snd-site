.block-template
  .container
    header.section-head
      h2
        span.text = t('.host_standard')
    article.article-box
      p = t('.update_date')
      p = simple_format t('.content')

    - t('.policies').each do |policy|
      = render_policies(policy)
